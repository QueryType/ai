"""Memory tool functions — pure Python, no framework decorators.

These are called by the game loop's tool dispatcher when the model requests them.
State mutations go directly into the GameState passed at dispatch time.
"""
